/* * ATM Example system - file BillsPanel.java     * * copyright (c) 2001 - Russell C. Bjork * */ package presentation;import application.domain.Money;import java.awt.*;import java.awt.event.*;/** The GUI panel that allows the operator to enter the number of bills in the *  ATM at startup */class DBPanel extends Panel{    /** Constructor     */    DBPanel()    {        setLayout(new GridLayout(0, 1, 0, 0));        setFont(new Font("Monospaced", Font.PLAIN, 14));                add(new Label("고객의 이름과 Card번호 PIN번호를 등록하시오.",                      Label.CENTER));                        DBNameField = new TextField(30);        DBNameField.addActionListener(new ActionListener() {            public void actionPerformed(ActionEvent e)            {                synchronized(DBPanel.this)                {                    DBPanel.this.notify();                }            }        });        Panel billsNumberPanel = new Panel();        billsNumberPanel.add(DBNameField);        add(billsNumberPanel);      }    /** Ask the operator to enter the number of bills in the cash dispenser     *     *  @return the number entered     */    synchronized int readDB()    {        boolean validNumberRead = false;        int billsNumber = 0;                DBNameField.setText("");                while(! validNumberRead)        {        	DBNameField.requestFocus();            try            {                wait();            }            catch(InterruptedException e)            { }                                try            {                billsNumber = Integer.parseInt(DBNameField.getText());                if (billsNumber >= 0)                    validNumberRead = true;                else                    getToolkit().beep();            }            catch(NumberFormatException e)            {                getToolkit().beep();            }            if (! validNumberRead)            {            	DBNameField.setText("Must be a valid integer >= 0");            	DBNameField.selectAll();            }        }        return billsNumber;    }        /** The field into which the number of bills is to be entered     */    private TextField DBNameField;}